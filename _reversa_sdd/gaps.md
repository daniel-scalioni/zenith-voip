# Lacunas Identificadas — zenith-voip

> Gerado pelo Revisor em 2026-06-19
> **Re-extração incremental — 2026-07-27**: GAP-01 fechado; 10 lacunas novas (GAP-RE-*) ao final
> Categorizadas por severidade

---

## Críticas (bloqueiam reimplementação)

| ID | Módulo | Descrição | Spec afetada | Status |
|----|--------|-----------|-------------|--------|
| GAP-01 | `audio` | `_detect_channel()` é stub — canal RX nunca detectado. Investigação (2026-06-19) mostrou que o protocolo real (`mod_audio_fork` stereo) intercala os dois canais no mesmo frame PCM16 — corrigido para de-interleaving em `src/audio/ingestor.py`. Convenção de qual canal é tx/rx ainda não validada contra FreeSWITCH real (🟡, ver `audio/design.md`) | `audio/requirements.md` | ✅ **Resolvida (2026-07-27)** — `_split_stereo_frame()` faz o de-interleaving PCM16 (pares=tx, ímpares=rx); convenção confirmada contra `mod_audio_stream` em chamada real (2026-07-23) |
| GAP-02 | `workers` | ~~`analyze_sentiment()` e `audit_procedure()` são stubs — e nem chegam a rodar~~ — **fechado (2026-08-21):** decisão do usuário foi remover o código morto em vez de implementar os stubs ou arquivar como débito técnico. `src/workers/post_call.py` deletado. Ver GAP-04 | `workers/requirements.md` | ✅ Fechada — código removido |

| GAP-11 | `infra` | Imagem `safarov/freeswitch:1.10.12` usada em `docker-compose.app.yml` **não contém `mod_audio_fork` compilado** — a gravação via fork de áudio não funcionava sem build customizado do módulo (`drachtio-freeswitch-modules`) | `infra/deployment` | ✅ Resolvida (2026-07-10, features `007-audio-stream-migration` + `008-piper-tts-standalone`) — `drachtio-freeswitch-modules` foi descontinuado; substituído por `mod_audio_stream` (`amigniter/mod_audio_stream`, MIT, ativo), build customizado sem depender do repositório SignalWire estar sempre disponível (`.deb` vendorizados). `zenith-api-1` buildado e rodando `healthy` pela primeira vez neste host (após GAP-18 remover os bloqueios de `requirements.txt`). Validação end-to-end feita via cliente WebSocket direto simulando o payload de `mod_audio_stream`: Redis Streams confirmou de-interleaving correto tx/rx. Gap adicional descoberto e corrigido no processo: a rota WebSocket `/audio-stream` nunca tinha sido registrada em `src/main.py` (`AudioIngestor.handle_forked_stream` existia mas não estava conectado a nenhum endpoint) — corrigido como `/audio-stream/{call_id}`. Também corrigido: FreeSWITCH roda em `network_mode: host` e não resolvia `zenith-api-1` por DNS de container — dialplan agora usa `${zenith_api_host}` (`127.0.0.1:8001`, `vars.xml`) |
| GAP-16 | `infra` | `freeswitch/conf/` não tinha config raiz bootável (faltavam `vars.xml`, `freeswitch.xml`, `acl.conf.xml`, `autoload_configs/event_socket.conf.xml`, `autoload_configs/sofia.conf.xml`, `directory/default.xml`) — container provavelmente não subia. Também: `modules.conf.xml` carregava `mod_esl` (módulo inexistente no FreeSWITCH; o módulo real do Event Socket é `mod_event_socket`) | `infra/deployment` | ✅ Resolvida (2026-06-24) — config completada e validada com boot real estável (90s observados, `RestartCount=0`) em `10.10.10.11`. Ver `_reversa_forward/004-bootstrap-freeswitch/` |
| GAP-17 | `infra`/`telephony` | `sip_profiles/internal.xml` declarava dois `<param name="sip-port">` (5060 e 5061) e referenciava certs TLS em `/etc/freeswitch/tls/` (inexistente) | `telephony/design.md` | ✅ Resolvida (2026-06-26) — TLS desativado (comentado), porta duplicada removida; profile `internal` vincula apenas porta 5060 |
| GAP-PROV-01 | `telephony` | `mod_xml_curl` não implementado — provisionamento dinâmico de ramais no FreeSWITCH ainda não existe | `telephony/design.md` | Aberta — ciclo futuro (`SIPExtension` + FastAPI + admin UI) |
| GAP-PROV-02 | `telephony` | Estratégia de importação em lote dos ramais do VitalPBX para o FreeSWITCH não definida | `telephony/design.md` | ✅ Resolvida (2026-06-26) — `scripts/import_extensions.py` lê CSV exportado do VitalPBX, gera `directory/extensions.xml` + `sip_profiles/upstream/*.xml`; dedup pjsip > sip; credenciais gitignored |
| GAP-UPSTREAM-01 | `telephony`/`infra` | 939 gateways upstream em **FAIL_WAIT** após deploy em 2026-06-29 — FreeSWITCH tenta registrar ramais no VitalPBX (`sip.maisalerta.tecnorise.com`) mas recebe rejeição. Causas prováveis: (1) IP `10.10.10.11` não liberado no firewall/whitelist do VitalPBX para registros SIP; (2) senhas do CSV divergem das configuradas no VitalPBX. Usuário não tem acesso admin ao VitalPBX — necessário acionar administrador para liberar IP | `telephony/design.md`, `infra/deployment/design.md` | ✅ Resolvida (confirmado em 2026-07-08, feature `006-registro-porta-vitalpbx`) — IP público `200.170.149.139` liberado no VitalPBX (feature `005-dynamic-external-ip`); `sofia status gateway upstream::upstream-1001` mostra `State: REGED`, `Status: UP (ping)`, uptime de ~10h no momento da verificação |
| GAP-12 | `telephony`/`database` | Nenhum handler de `CHANNEL_HANGUP` existia e nenhuma linha `Call` era criada em ponto algum do fluxo real (só em teste de schema) — gravação não tinha como ser persistida nem finalizada. Implementado `_handle_channel_hangup` + `src/services/calls.py` (cria `Call` no answer, finaliza no hangup, enfileira upload dos buffers) | `telephony/design.md`, `database/design.md` | Parcialmente resolvida — lógica testada via mocks (`tests/test_call_lifecycle.py`), pendente validação com Postgres/Redis reais |
| GAP-13 | `database` | `Call.metadata`, `Transcript.metadata` e `CallInsight.metadata` colidiam com o atributo reservado `metadata` do SQLAlchemy declarative, quebrando a importação de `src.database.database` (e `tests/test_multitenancy.py`) | `database/design.md` | ✅ Resolvida — renomeado para `extra_metadata` (coluna no banco continua `metadata`) |
| GAP-14 | `infra`/`workers` | Nenhum container do `docker-compose.app.yml` processava o job `upload_recording_batch` (só existia `arq-cleanup` para `audio_cleanup.WorkerSettings`) — mesmo com upload corrigido, a gravação nunca seria persistida. Adicionado serviço `arq-uploader` | `infra/deployment`, `workers/audio-upload` | ✅ Resolvida |
| GAP-15 | `workers` | Backend de storage trocado de S3 (nunca provisionado — `S3_ENDPOINT` vazio, sem MinIO no compose) para disco local (`RECORDINGS_PATH`, bind mount `./data/recordings`) — decisão de produto em 2026-06-22 | `workers/audio-upload`, `workers/audio-cleanup` | ✅ Resolvida — pendente validação ponta-a-ponta (Fase 4) |

## Moderadas (impactam qualidade)

| ID | Módulo | Descrição | Spec afetada | Status |
|----|--------|-----------|-------------|--------|
| GAP-03 | `api` | ~~Rate limit in-memory volátil — sem efeito após restart~~ — **fechado (2026-08-21)**: estado movido para Redis (`zenith:ratelimit:{ip}`, sorted set com TTL igual à janela de 60s), sobrevive a restart e é compartilhado entre `zenith-api-1`/`zenith-api-2` (antes cada instância tinha seu próprio limite in-memory). Fail-open só para `ConnectionError`/`TimeoutError` do Redis — outros erros (`ResponseError` etc.) propagam como bug real, não são engolidos como "infra indisponível" | `api/design.md` | ✅ Fechada |
| GAP-04 | `api`/`workers` | **Achado ampliado (2026-08-21):** não é "webhook fire-and-forget sem confirmação" — é a **cadeia pós-chamada inteira desconectada**, ponta a ponta. Nenhum caminho de execução real liga uma chamada a um webhook: (1) `src/workers/post_call.py::post_call_workflow` (`analyze_sentiment`/`audit_procedure`, GAP-02) não é enfileirado por ninguém (`enqueue_job` só existe em `audio_uploader.py`, para outro job) **e** não está registrado como worker em nenhum `docker-compose*.yml`/`Dockerfile*` — ao contrário de `audio_uploader`/`audio_cleanup`/`smb_sync`/`transcript_batch`, que estão todos com `command: ["arq", ...]` no compose; (2) mesmo que rodasse, publica no Redis Stream `call:post` (`REDIS_STREAM_POST_CALL`), que **não tem nenhum subscriber** em `src/`; (3) `WebhookDispatcher.dispatch_post_call` (`src/api/webhooks.py`) não tem nenhum chamador. Achado colateral: `src/workers/transcript_persist.py` (`TranscriptPersister`) também está órfão — sem chamador, sem teste, sem tocar desde o commit inicial do projeto (`cfd12b5`) — e foi superado pela feature `013-transcricao-persistida` (`transcript_batch.py`, esse sim registrado e ativo). Corrigido nesta rodada: specs que marcavam `webhooks.py`/`transcript_persist.py` como 🟢 (ativos) agora refletem 🔴 (desconectados) — ver `api/webhooks/design.md`, `workers/transcript-persist/design.md`, `api/design.md` (fluxo principal, passo 9, e riscos). Bug latente documentado mas **não corrigido** (código inalcançável): `webhooks.py::dispatch()` captura `httpx.RequestError`, não `httpx.HTTPStatusError` (não é subclasse) — uma resposta 4xx/5xx do receptor propagaria sem tratamento. **Fechado (2026-08-21):** decisão do usuário foi remover o código morto em vez de religar a cadeia ou arquivá-la como débito técnico — `src/workers/post_call.py`, `src/api/webhooks.py` e `src/workers/transcript_persist.py` deletados, `REDIS_STREAM_POST_CALL` removido de `config.py`. Se no futuro sentimento/auditoria/webhook pós-chamada entrarem em escopo, é implementação nova, não reativação — a política de retry/backoff/dead-letter do webhook segue sendo uma decisão de produto a tomar nesse momento, não antes | `api/design.md`, `api/webhooks/design.md`, `workers/post-call/design.md`, `workers/transcript-persist/design.md`, `workers/requirements.md` | ✅ Fechada — código removido |
| GAP-05 | `domain` | Personas de portaria remota não refletidas no modelo de domínio | `domain.md` | Aberta — **decisão do usuário (2026-08-21): fora de escopo por ora**, nenhuma feature atual depende disso; não modelar sem um caso de uso concreto puxando |
| GAP-06 | `telephony` | Auto-link SIP sem fallback se Redis vazio | `telephony/design.md` | Aberta — **decisão do usuário (2026-08-21): manter só o fallback manual (*88, TTL 120s)**, já existente; construir relookup automático via ESL não vale o esforço para um cenário raro (Redis só fica vazio se reiniciar no meio do dia) |
| GAP-30 | `telephony` | **Tronco de ligação externa (Algar) não passa pelo FreeSWITCH/Zenith — vai direto ao VitalPBX.** Descoberto em 2026-08-23 ao esclarecer o modelo de domínio real com o usuário (contexto que corrigiu o entendimento equivocado do GAP-RE-03 sobre "ramal local"). Esse tronco é usado para: (1) ligar ao celular do morador quando o ATA do apartamento não atende (autorizações), (2) ligar a zeladores/porteiros/síndicos dos condomínios — ligações que hoje **não são capturadas nem transcritas**, apesar de serem, segundo o usuário, parte central do "primeiro entregável" (análise de qualidade do atendimento, que depende de filtrar ligações pelos ramais dos atendentes). Diferente de GAP-RE-03 (variável de canal não populada para ramal já dentro do FreeSWITCH), este é uma lacuna de **roteamento**: a chamada nem chega a passar pelo B2BUA. Nenhum código, spec ou dialplan deste projeto referencia "Algar" hoje (confirmado via grep). Sem investigação de causa/solução ainda — registrado para não perder o achado | `telephony/design.md` (a criar/expandir) | Aberta — a investigar |

## Cosméticas

| ID | Módulo | Descrição | Spec afetada | Status |
|----|--------|-----------|-------------|--------|
| GAP-07 | `ai` | Contagem de keywords corrigida (13→27) | `ai/requirements.md` | ✅ Resolvida |
| GAP-08 | `c4` | Typo `api_fastagi` → `api_fastapi` | `c4-context.md` | ✅ Resolvida |
| GAP-09 | `stt` | Timeout descrito como hardcoded (é configurável) | `services/stt/design.md` | ✅ Resolvida |
| GAP-10 | `telephony` | Eventos ESL incompletos (faltavam SOFIA events) | `telephony/requirements.md` | ✅ Resolvida |
| GAP-18 | `infra`/`requirements.txt` | `zenith-api-1`/`zenith-api-2` nunca foram buildados com sucesso neste host (10.10.10.11) antes de 2026-07-09 — descoberto ao tentar deployar para validar a feature `007-audio-stream-migration`. `requirements.txt` acumulava pins mortos (`greenswitch==1.1.0`, `redis-streams==0.6.0`, `python-brasilcpf==2.0.0`, todos removidos), um conflito real de dependências (`arq` exige `redis<6`, `langgraph-checkpoint-redis` exige `redis>=6.2.0`, resolvido trocando `RedisSaver` por `MemorySaver`), e `piper-tts==1.2.0` dependente de `piper-phonemize~=1.1.0` (sem wheel disponível) | `requirements.txt`, `src/ai/consensus_graph.py`, `src/services/tts_service.py` | ✅ Resolvida (2026-07-09, feature `008-piper-tts-standalone`) — `piper-tts` atualizado para `1.4.2` (`OHF-Voice/piper1-gpl`, PyPI, sem `piper-phonemize`). `pip install --dry-run -r requirements.txt` resolve 100% sem erros. `zenith-api-1` segue não validado ponta a ponta (build completo da imagem da aplicação), mas todos os bloqueios de dependência conhecidos foram removidos |
| GAP-19 | `database` | `Transcript.is_final`, `CallInsight.anomaly_detected`, `STTMetric.success`/`fallback_activated` em `src/database/models.py` eram declarados como `Column(default=...)` **sem tipo** (compila para `NullType`) — `create_tenant_schema()` falha ao gerar DDL (`CompileError: Can't generate DDL for NullType()`), nunca detectado porque nenhum schema de tenant tinha sido provisionado de fato antes | `database/design.md` | ✅ Resolvida (2026-07-10, MVP Fase 1) — corrigido para `Column(Boolean, default=...)`, consistente com `sa.Boolean()` já usado em `alembic/versions/003_tenant_schema_tables.py`. Descoberto e corrigido ao rodar `scripts/provision_tenant.py` pela primeira vez contra o schema `tenant_akom` |
| GAP-20 | `workers` | `src/workers/audio_cleanup.py` nunca tinha rodado com sucesso como container: `cron(run_cleanup, ..., run_on_startup=...)` usa kwarg inexistente na API do `arq` (correto é `run_at_startup`), e `WorkerSettings.redis_settings = settings.REDIS_URL` passa a DSN como string crua em vez de `RedisSettings.from_dsn(...)` (padrão já usado corretamente em `audio_uploader.py`) | `workers/audio-cleanup/design.md` | ✅ Resolvida (2026-07-10, MVP Fase 1) — ambos corrigidos; `arq-cleanup` sobe e permanece rodando (antes ficava em crash loop) |
| GAP-21 | `telephony`/`database` | `tenant_id`/`pbx_id` nunca eram populados no dialplan para ramais registrados nativamente no FreeSWITCH (não via `bypass_to_pbx` da VitalPBX) — `scripts/import_extensions.py` não escrevia esses campos no diretório gerado. Resultado: nenhuma linha `Call` era criada (guard `if tenant_id:` em `esl_client.py`) e a gravação de áudio caía numa pasta de tenant vazio, órfã, sem rastreabilidade | `database/multitenancy/design.md`, `telephony/design.md` | ✅ Resolvida (2026-07-10, MVP Fase 1) — provisionado o primeiro tenant real (`Akom`, schema `tenant_akom`, via `scripts/provision_tenant.py`) e fixado `tenant_id=akom`/`pbx_id=<uuid>` em `freeswitch/conf/vars.xml` |
| GAP-22 | `api`/`audio` | **Segurança.** WebSocket `/audio-stream/{call_id}` (`src/main.py`) aceitava qualquer `call_id`, inclusive inventado, sem checar se foi de fato registrado via evento ESL `CHANNEL_ANSWER` real — nenhuma autenticação, nenhuma verificação de origem. Agravado por `fastapi-1`/`fastapi-2` publicarem a porta em `0.0.0.0` (não só loopback), apesar do comentário em `vars.xml` sugerir acesso só local. Achado via `/security-review` em 2026-07-12 | `audio/audio-ingestion/design.md` | ✅ Resolvida (2026-07-12) — `handle_forked_stream` (`src/audio/ingestor.py`) agora rejeita (`close(code=4401)`, antes do `accept()`) qualquer `call_id` ausente de `self.stream_metadata`; portas de `fastapi-1`/`fastapi-2` restritas a `127.0.0.1` em `docker-compose.app.yml` |
| GAP-23 | `telephony` | `esl_client` (`src/telephony/esl_client.py`) **nunca era conectado em lugar nenhum do código** — nem `connect()` nem `start_event_listener()` eram chamados. Descoberto ao validar o fix de GAP-22: sem isso, `CHANNEL_ANSWER`/`CHANNEL_HANGUP` nunca eram processados de verdade para nenhuma chamada real, então `register_stream_metadata()` nunca populava `audio_ingestor.stream_metadata` — o pipeline de gravação (features `005`-`008`) nunca tinha sido validado ponta a ponta com uma chamada real de fato, só com simulação direta de WebSocket (bypass do ESL) | `telephony/esl-integration/design.md` | ✅ Resolvida (2026-07-12) — `esl_client.start_event_listener()` chamado no `lifespan` de `src/main.py`, só na instância `INSTANCE_ID=1` (evita `fastapi-1`/`fastapi-2` processarem o mesmo evento em duplicidade, já que `create_call_record` não é idempotente). Conexão TCP confirmada ativa (`ss -tnp` no host: `172.21.0.1:8021 ESTAB`). Validação ponta a ponta com chamada real feita em 2026-07-22 (ver GAP-24) |
| GAP-24 | `infra` | **Imagem de produção do FreeSWITCH rodava sem `mod_audio_stream` carregado** — o build customizado (`freeswitch/Dockerfile`) nunca tinha sido promovido a produção (só existia validado numa imagem de teste separada, `zenith-freeswitch-audiostream:test`); `zenith-voip-freeswitch:latest` em produção era a imagem base vanilla, zero camadas próprias. Passou despercebido por 24h porque o serviço `freeswitch` não tinha `healthcheck:` próprio — só herdava o genérico da imagem base, que não sabe nada sobre módulos do projeto | `infra/deployment/design.md` | 🟡 Parcialmente resolvida (2026-07-22) — gate de build (`RUN test -s .../mod_audio_stream.so`) e `healthcheck:` próprio no compose (`module_exists mod_audio_stream`) implementados e **rebuild + redeploy real já feito em produção** (10.10.10.11): `module_exists mod_audio_stream` → `true`, container `healthy`, gateway `upstream-1001` seguiu `REGED`/`UP` sem regressão. No processo, reconciliado o desalinhamento de `main` no servidor (HEAD destacado numa tag antiga, 18 commits atrás — as branches `009-api-invocation-via-esl-client`/`010-record-real-call-audio-e2e`, já validadas com chamada real em 2026-07-14/15 mas nunca merged, foram trazidas para `main`) e corrigido um bloqueio de build real (downgrade de `libcurl4` nos `.deb` vendorizados, precisa de `--allow-downgrades`). Decisão validada via consulta multi-LLM (`/brainstorming-multiagent`, duas rodadas) antes de implementar. Risco residual assumido conscientemente: `.deb` vendorizados são do FreeSWITCH 1.11.1, mas o runtime é 1.10.12 — sem garantia de ABI estável entre minors, a ser observado na próxima chamada real. **Validado com chamada real em 2026-07-23**: pipeline de software completo confirmado ponta a ponta pela primeira vez — `CHANNEL_ANSWER` processado, `uuid_audio_stream` disparado via `bgapi`, WebSocket `/audio-stream/{call_id}` aceito, chunks publicados em `call:events` (Redis). No processo, descoberto que `zenith-api-1`/`zenith-api-2` estavam com 35h de uptime sem reiniciar — o código do merge 009/010 já estava no disco (bind mount `./src:/app/src`), mas o processo Python em memória (sem `--reload`) ainda era o antigo; corrigido com `docker compose restart`, sem necessidade de rebuild. **Ainda pendente**: `GAP-NET-01` (RTP quase nulo — só 2 pacotes em toda a chamada, ambos saindo do FreeSWITCH sem retorno; achado novo: uma das pernas leva a um IP privado `192.168.50.92` anunciado pelo próprio softphone 3CX do ramal 1001, que o `rtp-auto-adjust` do profile `internal` não corrigiu por falta de RTP de entrada para disparar o auto-adjust — ver `telephony/design.md`) |
| GAP-25 | `database` | `public.tenants` estava vazio e o schema `tenant_akom` não existia em nenhum volume Postgres do host — o provisionamento de tenant de 2026-07-10 (GAP-21) nunca persistiu de fato ou foi perdido. Sintoma: `create_call_record` falhava com `relation "calls" does not exist`, capturado e logado pelo tratamento de exceção do merge 010, sem interromper a captura de áudio | `database/multitenancy/design.md` | ✅ Resolvida (2026-07-23) — reprovisionado via `scripts/provision_tenant.py` (mesmos parâmetros); novo `pbx_id` (`c5bf3191-75b4-4a45-b5e1-c9b7942f8176`) atualizado em `freeswitch/conf/vars.xml` |
| GAP-26 | `infra` | `DATABASE_URL` de `zenith-arq-transcript`/`zenith-arq-cleanup` no `.env` de produção (10.10.10.11) apontava para a porta **5433** — porta que só existe como mapeamento externo host→container do `zenith-postgres-candidate` (`"5433:5432"`), não ouvida dentro da rede `ai-hub-net`. `zenith-api-1`/`zenith-arq-uploader` usavam a porta correta (5432) só porque foram recriados num `docker compose up` com o shell exportando `DATABASE_URL` certo — mascarando a divergência do `.env` em disco até a feature `013-transcricao-persistida` recriar `arq-transcript`/`arq-cleanup` sem esse export, gerando `ConnectionRefusedError` best-effort (RN-04) silencioso em toda tentativa de persistir `Transcript` | `infra/deployment/design.md` | ✅ Resolvida (2026-08-19, feature `013-transcricao-persistida`) — `.env` corrigido para 5432 (backup salvo), containers recriados via `docker compose up -d --no-deps arq-transcript arq-cleanup`. Ver `_reversa_forward/013-transcricao-persistida/regression-watch.md#W001` |
| GAP-29 | `infra` | **Suspeita de conflito de subnet Docker com a LAN real do host.** Apenas a rede `ai-hub-net` (`docker-compose.infra.yml`/`docker-compose.app.yml`) tinha subnet IPAM explícita (`172.21.0.0/16`); as demais redes do projeto (`zenith-quality-test-net`, `zenith-quality-rehearsal-net`, `zenith-quality-candidate-net`, e a já removida `zenith-feature-012-spike-net`) dependiam de alocação automática do Docker. Em 2026-08-19, logo após `docker network rm zenith-feature-012-spike-net` (limpeza de containers órfãos da feature 012), a conectividade SSH com o host (`10.10.10.11`) caiu e passou a intermitente | `infra/deployment/design.md` | ✅ **Hipótese não confirmada (2026-08-20)** — SSH normalizou sozinho; inspeção real do host (`docker network ls` + `ip route`) mostra todas as redes `zenith-*`/`ai-hub-net` em `172.21.0.0/16`–`172.26.0.0/16` (auto-alocadas, inclusive as três "sem subnet explícita"), sem overlap algum com a LAN `10.10.10.0/24`. A mitigação de subnets `/24` explícitas continua só no repo (não aplicada no servidor) — decisão consciente de **não aplicá-la**: recriar redes ativas por uma causa que a evidência não sustenta é mais risco do que ganho. Causa raiz da queda de SSH em 2026-08-19 permanece desconhecida, mas não é esta |
| GAP-28 | `infra` | **Container Postgres legado órfão ainda existe no host** (`zenith-postgres`, volume `zenith-voip_zenith_postgres_data`), desconectado de toda rede desde o cutover W002 da feature `012` (2026-08-18) — é literalmente o banco mencionado na spec como "não pode ser reintroduzido no alias". Contém dados reais congelados de antes do cutover: 12 `calls` em `tenant_akom`, 0 `transcripts` (a feature de transcrição ainda não existia), última chamada em 2026-08-01. Confirmado que nada escreve nele hoje (as duas chamadas de teste da feature `013`, geradas em 2026-08-19, não aparecem ali) | `infra/deployment/design.md` | ✅ Resolvida (2026-08-20) — **correção de fato**: o container `zenith-postgres` já não existia mais no host (só o volume órfão sobrava); e o volume **não continha as 12 chamadas** — essas 12 já estavam (e seguem) na produção real (`zenith-postgres-candidate`/`zenith-postgres`, confirmado por `call_id`/`started_at` idênticos). O volume órfão continha só `public.tenants`/`public.pbxs` de antes do schema-per-tenant, 45,8 MB, verificado via instância `postgres:16-alpine` temporária `--network none` (leitura, sem tocar produção) antes de remover. Decisão do usuário: remover (`docker volume rm zenith-voip_postgres_data`). Sem perda de dado |
| GAP-27 | `infra` | **Colisão de nome latente entre produção e rehearsal de qualidade.** `docker-compose.infra.yml` (produção) e `docker-compose.candidate.yml` (rehearsal isolado do gate de qualidade) declaravam o mesmo `container_name` (`zenith-postgres-candidate`) **e o mesmo nome de volume** (`zenith-postgres-candidate-data`). | `infra/deployment/design.md` | ✅ Resolvida (2026-08-20, [ADR-012](adrs/012-promover-nome-canonico-zenith-postgres.md)) — a parte já corrigida em 2026-08-19 (rehearsal isolado com nomes próprios) segue valendo; a parte pendente (produção com nome `-candidate`) foi fechada depois que o GAP-28 removeu o volume órfão legado: sem `zenith-postgres` restando em lugar nenhum (nem serviço no Compose, nem volume), a proteção que justificava manter o sufixo deixou de ter objeto. Produção renomeada para `zenith-postgres`/`zenith-postgres-data` (compose + teste `test_postgres_cutover_compose.py` atualizados; corte ao vivo pendente de execução coordenada, ver ADR-012). Débito cosmético da rede `ai-hub-net` sem prefixo `zenith_` explícito permanece, sem ganho funcional em corrigir |


---

## Lacunas identificadas na re-extração de 2026-07-27

Achados novos, produzidos pela comparação entre o código atual (`0658157`) e a extração de
2026-06-19 (`48da5b1`). Não são regressões das features do ciclo forward — são pontos que a
releitura do código expôs.

### Críticas

| ID | Módulo | Descrição | Spec afetada | Status |
|----|--------|-----------|-------------|--------|
| GAP-RE-01 | `api`/`telephony` | **HA é parcial.** Só `INSTANCE_ID == 1` consome o event stream ESL (`main.py`). Se `fastapi-1` cair, `fastapi-2` continua servindo HTTP normalmente e **nenhuma chamada é gravada** — sem alarme, sem failover. O `depends_on`/healthcheck do compose não cobre esse cenário porque `fastapi-2` está saudável | `api/design.md`, `infra/deployment` | Aberta |
| GAP-RE-02 | `database` | ~~`CallStatus.failed` e `CallStatus.ringing` não são atribuídos por nenhum caminho de código~~ — **fechado (2026-08-21)**: `CHANNEL_HANGUP` agora classifica `completed`/`failed` pelo `Hangup-Cause` em toda chamada finalizada. `CHANNEL_CREATE` cria a linha em `ringing` quando `variable_zenith_tenant_id` já vem no evento — hoje cobre tronco ATA. Um fallback `global_getvar` para cobrir ramal local também foi tentado e **revertido**: gerava linha `ringing` órfã (ramal local nunca seta `zenith_*` em nenhum evento do ciclo de vida, então `mark_call_in_progress`/`finalize_call_record` nunca a encontrariam de volta) — ramal local segue sem `ringing`, é o mesmo residual do GAP-RE-03. Guard `Call-Direction == "inbound"` evita linha órfã também na perna B de toda chamada bridgeada (achado com evento real do FreeSWITCH em produção antes do merge). Ver `state-machines.md` | `database/design.md`, `state-machines.md` | ✅ Fechada |
| GAP-RE-03 | `telephony`/`database` | Chamada sem `tenant_id` populado **não gera linha nenhuma** (guard `if tenant_id:`). Perda silenciosa: não há como distinguir "não houve chamada" de "houve chamada não registrada". **Mitigado parcialmente (2026-08-21):** a parte "não há métrica nem log de contagem desse descarte" foi fechada — `call_dropped_no_tenant_total` incrementa e loga em `_handle_channel_answer` quando a perna A chega sem `tenant_id` (filtrado por `Call-Direction` pra não contar a perna B, que nunca tem `tenant_id` por design). A causa-raiz (ramal local nunca tem `zenith_tenant_id` em evento nenhum, porque `local_extension`/`echo_test` não passam por `zenith_audio_fork`) segue aberta — uma tentativa de fallback `global_getvar` no CHANNEL_CREATE para resolvê-la foi revertida por criar linha `ringing` órfã (ver GAP-RE-02). **Correção de modelo de domínio (2026-08-23):** o plano registrado em `telephony/design.md` (via `/brainstorming-multiagent`) presumiu que "`local_extension` = ramal-a-ramal entre atendentes, chamada rara/pouco importante". O usuário corrigiu: ramal de atendente **não é registrado no FreeSWITCH** (fora do escopo de `local_extension`); os ramais `1\d{3}` que essa extension cobre são de **condomínio**, que devem sim ser gravados. Além disso, existe uma lacuna maior e não relacionada — GAP-30 (tronco Algar de ligação externa não passa pelo FreeSWITCH) — que pode ser mais prioritária que este fix pontual de dialplan. Plano de `telephony/design.md` não revisado ainda com esse entendimento correto — revisar antes de implementar | `telephony/design.md` | Aberta (mitigada) |

### Moderadas

| ID | Módulo | Descrição | Spec afetada | Status |
|----|--------|-----------|-------------|--------|
| GAP-RE-04 | `workers`/`infra` | ~~tmpfs de 512 MB sem backpressure~~ — **fechado (feature `014-captura-wav-16k`)**: `src/audio/capacity.py::RecordingCapacityGuard` projeta uso do filesystem (reservas ativas + processamento pendente + headroom) e aplica admissão com histerese — recusa novas gravações abaixo de 20% livre, retoma a 30%. `record_recording_refused()`/`recording_degraded_mode` (Prometheus) cobrem o "sem alarme agregado". Estratégia é admissão preventiva, não "descarte do mais antigo" como o gap original sugeria — evita o overfill em vez de liberar espaço depois, mesmo objetivo por caminho diferente. `gaps.md` estava desatualizado em relação a `workers/design.md`/`requirements.md`, já ✅ há mais tempo | `workers/audio-upload/design.md` | ✅ Fechada |
| GAP-RE-05 | `audio` | ~~`AudioChunk.timestamp` é sempre `0.0` — nunca populado~~ — **obsoleto (confirmado 2026-08-21)**: a classe `AudioChunk` não existe mais em `src/audio/` — feature `014-captura-wav-16k` substituiu o buffer de chunks em memória por escrita direta em WAV PCM16 na origem, eliminando estruturalmente o problema de reconstrução de eixo temporal que o gap descrevia | `audio/design.md` | ✅ Fechada — obsoleto |
| GAP-RE-06 | `sidecar`/`telephony` | ~~O `ip-watcher` reintroduziria o IP público nos profiles internos~~ — **avaliação revista em 2026-07-27**: os profiles `internal.xml`, `internal-7060.xml` e `internal-5062.xml` usam `$${local_ip}` **literal** (linhas 18-19 de cada), não a variável `external_*_ip`. O que o watcher escreve só é consumido por `upstream.xml`, que fala com o VitalPBX pela internet e deve mesmo anunciar IP público. O fix do GAP-NET-01 desacoplou os dois. **Decisão do usuário (2026-07-27): o watcher roda no deploy atual.** | `sidecar/design.md` | ✅ Fechada — sem conflito |
| GAP-RE-07 | `telephony`/`database` | `tenant_id`/`pbx_id` têm **duas fontes de verdade**: `public.tenants`/`pbxs` no Postgres e as variáveis globais de `freeswitch/conf/vars.xml`. Se divergirem (como já ocorreu no GAP-25), chamadas gravam com `pbx_id` inválido sem erro visível. **Mitigada (2026-08-21, decisão do usuário: Postgres é a fonte de verdade):** `scripts/sync_vars_xml.py` gera as duas linhas de `vars.xml` a partir do Postgres (substituindo a edição manual), com `--check` para detectar divergência sem escrever. **Validado contra produção (2026-08-23, autorizado pelo usuário):** rodado via `docker compose exec fastapi-1` (ponte manual — `vars.xml` só está montado no container `freeswitch`, o Postgres só é alcançável a partir dos containers `fastapi-*`; copiado `vars.xml` + script para `/tmp` dentro de `fastapi-1`, `--check` confirmado, copiado de volta) contra o Postgres real de 10.10.10.11. Resultado: `vars.xml` **já estava sincronizado** — `pbx_id=c5bf3191-...` da correção manual do GAP-25 continua batendo com `public.pbxs`. Nenhuma escrita necessária, `reloadxml` não precisou rodar. Backup do `vars.xml` pré-verificação em `.deploy-backups/vars.xml.bak-20260823-202356` no servidor. **Achado colateral:** `public.tenants` de produção tem um segundo tenant, `tenant_test_t046_synthetic` (`test-t046.invalid:5060`) — parece resíduo de teste (nome sugere feature 012/T046) vazado em produção; não removido nesta ação (fora do escopo autorizado), registrar para limpeza futura. **Ainda "mitigada", não "fechada":** o mecanismo automatizado existe e foi validado, mas a correção real (GAP-25) continua tendo sido manual — não há ainda nada rodando o `--check` periodicamente para pegar a próxima divergência sozinho | `database/multitenancy/design.md` | Aberta (mitigada, validada em produção) |
| GAP-RE-08 | `observability` | ~~As métricas Prometheus de S3 continuam registradas e expostas, mas medem um subsistema que não existe mais (ADR-009)~~ — **obsoleto (confirmado 2026-08-21)**: `src/observability/telemetry.py` não registra nenhuma métrica de S3 hoje (`stt_latency`, `stt_fallback_counter`, `llm_latency`, `calls_active`, `call_duration`, `consensus_decisions` — nenhuma relacionada a S3). Foram removidas em algum ponto após a migração para tmpfs; o gap descrevia um estado que não é mais o atual | `observability/design.md` | ✅ Fechada — obsoleto |

### Cosméticas / a investigar

| ID | Módulo | Descrição | Spec afetada | Status |
|----|--------|-----------|-------------|--------|
| GAP-RE-09 | `extraction` | ~~`python-brasilcpf` foi removido do `requirements.txt` (GAP-18). Verificar se a extração de CPF perdeu a validação de dígito verificador~~ — **fechado (2026-08-21)**: confirmado que sim, tinha perdido; `src/extraction/regex_layer.py::_is_valid_cpf` implementa o cálculo do dígito verificador (módulo 11) e rejeita sequências repetidas, aplicado em `RegexExtractor.extract`/`has_suspicion` | `extraction/design.md` | ✅ Fechada |
| GAP-RE-10 | `services` | ~~`PiperTTS.synthesize()` mantém `voice` e `speaker_id` na assinatura, mas ignora ambos~~ — **fechado (2026-08-21)**: assinatura corrigida para `synthesize(self, text: str, **kwargs) -> bytes`, sem parâmetros que mentem sobre capacidade; `**kwargs` mantido só por compat com `TTSStrategy` | `services/design.md` | ✅ Fechada |

### Observação de arquitetura (não é gap)

`ESLClient` acumulou orquestração do ciclo de vida da chamada: em dois handlers ele fala com
`audio`, `services`, `database` e `workers`. 🟡 Aproxima-se do anti-padrão *God Class*
listado no `CLAUDE.md`. Candidato natural a extração de um `CallLifecycleService` se os
handlers continuarem crescendo.
